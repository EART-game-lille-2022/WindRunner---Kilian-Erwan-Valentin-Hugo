using UnityEngine;

[ExecuteInEditMode]
public class WindWeigth : MonoBehaviour
{
    public float value;

    private void Update()
    {
        //WindPoint.GetWeightAt(transform.position, out value, out Vector3 scale, out Vector3 forward);

        //transform.localScale = scale;
        //transform.rotation = Quaternion.LookRotation(forward);
    }
}